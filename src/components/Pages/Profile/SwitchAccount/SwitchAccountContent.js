import React from 'react';
import { Grid } from '@material-ui/core';

const SwitchAccountContent = ({
    EN,
    user,
    type,
    handleChange,
}) => {
    return (
        <Grid container spacing={0} className='switch-account-content'>

        </Grid>
    )
};

export default SwitchAccountContent;